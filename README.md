# closekin.r-universe.dev

This is a probably-temporary placeholder Runi repo, mainly to test cross-repo dependencies. It contains one dummy package 'crossrepotest' (it worked!), and a temporary 'kinference' until that gets released "officially" on a different Runi repo.
