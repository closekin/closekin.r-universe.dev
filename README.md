# closekin.r-universe.dev

This is a probably-temporary placeholder Runi repo, mainly to test cross-repo dependencies. It will contain one dummy package 'crossrepotest', and if I can get that to work then a temporary 'kinference' until that gets released "officially" on a different Runi repo. I was trying to call this repo "pushingjellyupamountain.r-universe.dev" to convey something of the flavour of the process, but that turns out to be a bit tricky, so it's got a boring name instead :)
